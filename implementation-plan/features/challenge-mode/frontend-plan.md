# Retired: Challenge Mode Frontend Plan

The former challenge-mode frontend is preserved but unreachable as of v2.8.0.
Every primary navigation implementation renders `ChallengeComingSoonScreen`,
and old challenge links are intercepted before preview or authentication.

Use:

- `implementation-plan/features/challenge-mode/README.md` for product behavior.
- `implementation-plan/frontend-plan.md` for current navigation and gameplay.
- `app/screens/Challenge*.tsx` only as dormant redesign reference.
